# Clawd 原型未落地清单 + 移植手册

日期：2026-08-21
基准原型：`diagnostics/clawd-合并原型.html`（43 个姿势 + 6 个附加系统）
基准实装：`dist-extension/claude-web-test/index.js`（2.0.144 测试版）

> 2026-08-21 Codex 更新：§2.1 的 12 个无需新帧姿势已经全部接入实际状态与触发，
> 自动化回归通过，并在 Android Via 实测 `tilt` / `wow`。本文保留原始移植表，
> 但它们已经不再属于未落地项。

这份文档回答两件事：**还差什么**，以及**怎么把一个姿势从原型搬进扩展**。
第二部分比第一部分重要——移植的做法这两轮踩出来了，照着走能省掉大半天。

---

## 1. 已经落地的，别重做

**姿势（34 个）**

```
think  stream  done  stop  err          A 轨（生成）
idle   sleep                             B 轨（在场）
grab   drag    fly   land  stomp         A2 物理
turn   face    t1 t2 t3 t4 t5            A2 戳与生气
peek   scroll                            滚动态（扒住输入框）
heart                                    连点彩蛋
sit                                      长生成后的离散疲劳态（A 轨）
tilt  wow                                输入问号 / 感叹号或长文本（B 轨）
hide  ledge  tramp                       低频界面在场动作（B 轨）
shake around spin dhop lean              低频空闲动作（B 轨）
wake                                     真正睡醒时的过渡（B 轨）
```

**姿势之外**

- A2 全套物理：抓起 / 拖动 / 抛掷 / 空气墙 / 天花板 / 落地压扁 / 戳的 5 档 / 生气序列
- 三轨状态机（C > A > B）、序列锁、代次令牌、烦躁衰减
- 输入态：聚焦 → 低头看；有文字 → 一边看一边点头
- 滚动态：滚动开始扒住输入框，停下探回来
- 落地后如果盖住右上角的装饰 Clawd，会轻轻滑开
- 冷落（neglected）、打盹（drowsy）、入睡过渡、害羞（shy ambient）

**帧**：8 个样式表现在各有 **32 帧**（原来 15 + A2 的 14 + peek 的 3）。

---

## 2. 还没落地的

### 2.1 已完成：不用加新帧的（12 个）

这几个用的帧扩展里全都有。现在规则、`@keyframes` 和实际触发都已经接好：

| 姿势 | 原型里是什么 | keyframes |
|---|---|---|
| `sit` | 累了坐下（生成久了的疲劳态） | 0（只换帧） |
| `tilt` | 歪头，「？」 | 0（只换帧） |
| `wow` | 惊讶，「！」 | 1 |
| `hide` | 整只沉进输入框里躲起来 | 1 |
| `ledge` | 扒住边缘吊一下（`peek` 的兄弟，转 -24° 挂着） | 1 |
| `tramp` | 蹦床式弹跳 | 1 |
| `shake` | 甩身子 | 1 |
| `lean` | 侧靠，「？」 | 2 |
| `around` | 东张西望，「？」 | 2 |
| `spin` | 转圈，「♪」 | 2 |
| `dhop` | 双跳，「♪」 | 3 |
| `wake` | 醒来 | 2 |

落地后的触发分工：

- `sit`：流式生成持续 12 秒后接管 A 轨。当前是离散坐下；连续疲劳仍归 `applyTired`。
- `tilt`：输入框聚焦且内容包含问号。
- `wow`：输入框聚焦且内容包含感叹号，或去空白后的长度达到 20。
- `wake`：只在真正从 `idleAsleep` 恢复时播放 500ms，普通 focus/input 不重复弹。
- `around / shake / spin / dhop / lean / hide / ledge / tramp`：完全空闲时每 22–36 秒
  低频轮换；欢迎页、输入框聚焦、有文本、生成、抓取或睡眠状态下不触发。

全部姿势只画在 `.clawd-composer-clawd` 上。消息末尾的小 Clawd 仍消费同一套
A/B/C 数据，但保持落款职责，不套这批大动作。

### 2.2 要先加帧的（9 个）

加帧走 `tools/clawd-inject-frame.py`，用法见 §3.1。

| 姿势 | 缺的帧 |
|---|---|
| `crouch` | `crouch-lo` `crouch-up` |
| `scratch` | `scratch-hi` `scratch-mid` |
| `stretch` | `stretch-out` |
| `yawn` | `yawn-arms` `yawn-open` |
| `nodoff` | `doze-lo` |
| `nudge` | `arm-r-mid` `push-arm` |
| `walk` | `walk-l-lo` `walk-l-up` `walk-r-lo` `walk-r-up` |
| `point` | `arm-r-1` `arm-r-3` `arm-r-mid` `point-r` `point-r2` |
| `facepalm` | `arm-l-1` `arm-l-3` `arm-l-hi` `arm-l-mid` `facepalm` `facepalm-mid` |

> `walk` 在 2.0.40 被删过一次（踱步）。要不要再做回来是产品决定，不是技术债。

### 2.3 六个附加系统，一个都没做

这几个不是姿势，是**跨姿势的修饰层**，做起来比单个姿势重得多：

| 系统 | 原型里做什么 | 备注 |
|---|---|---|
| `.sym` 符号层 | 头顶飘一个字符：`… ! z ? 哼 ♪ ♥ ✦ ～`，跟姿势绑定 | 要新增一个 DOM 节点。⚠ 见 §4 关于 `#send_form` 子节点的坑 |
| `.shadow` 地面影子 | 脚下一团影子，落地时跟着压扁（`landSh`） | 同上。少了它落地的冲击感会弱一截 |
| `applyTired` 疲劳 | 生成越久越累：出字的跳高变矮、周期变长、身子下沉、眼睛半闭 | 连续量，喂给 `--hop` `--hd` `--sag` `--shk` `--fA/--fB` |
| `applyRush` 冲刷 | 滚动越猛，动作幅度越大，还会甩速度线 | 依赖 `.streak` 节点 |
| `.streak` 速度线 | 快速滚动时身后甩出的短线 | 依赖上一条 |
| `startWalk` 踱步 | 沿着输入框上沿真的走动 | 需要 `walk-*` 四帧 |

`applyTired` 是这六个里最值得做的：它给「生成很久」这件事一个渐变的表达，
而现在生成态只有 think/stream 两个离散姿势。

---

## 3. 移植手册

### 3.1 加帧

```powershell
Set-Location -LiteralPath 'E:\Crab Pot\项目\酒馆\Claude-Clawd-酒馆助手'
python tools\clawd-inject-frame.py --list                      # 看还缺哪些
python tools\clawd-inject-frame.py --frames yawn-open,yawn-arms --dry-run
python tools\clawd-inject-frame.py --frames yawn-open,yawn-arms
python tools\clawd-inject-frame.py --frames yawn-open,yawn-arms --verify
```

脚本会自动处理三件容易出错的事：8 个样式表逐字节一致、compat 那 4 个是压缩过的
（整份一行且块尾没有分号，要用压缩写法插并补分号）、以及各文件原有的换行符。

**帧数据住在 8 个样式表里，姿势规则住在 `index.js`。** 别搞反。

部署到本机酒馆时目标是 `claude-web-test2`（不是 `claude-web-test`，那是个空壳）：

```powershell
python tools\clawd-inject-frame.py --frames <帧名> `
  --target-dir 'D:\SillyTavern-1.18.0\public\scripts\extensions\third-party\claude-web-test2\styles'
```

### 3.2 写姿势规则

规则和 `@keyframes` 都加进 `index.js` 里 `installStyle()` 的那段模板字符串
（`<style id="claude-clawd-interaction-style">`），跟现有的 `clawd-a2-*` 放一起。

**节点分工——这条是整套写法的地基：**

| 通道 | 归谁 | 写在哪 |
|---|---|---|
| `transform` on button | A2 的位置、旋转、落地压扁 | JS（`a2Place`） |
| `translate` on button | 输入态的抬起 3px | CSS |
| `rotate` on button | 暂时空着，留给以后的倾斜 | — |
| `transform` on `::before` | 姿势本身（位移/缩放/旋转） | CSS `@keyframes` |
| `translate` on `::before` | 姿势里的纯上下位移（点头、扒住） | CSS `@keyframes` |
| `box-shadow` on `::before` | 换帧 | CSS `@keyframes`，必须 `step-end` |

`transform` 和 `translate`/`rotate`/`scale` 是**不同的属性**，会自动叠加，
不用担心互相覆盖。这就是为什么扩展里一个 `@property` 都不需要——
原型用 7 个 `@property` 合成 transform，是因为它把所有东西堆在同一个节点上。

**选择器模板：**

```css
/* C 轨姿势（被抓、被丢、戳、生气）挂 data-clawd-c —— renderClawdTracks 已经在写它 */
button.${BUTTON_CLASS}[data-clawd-c="yawn"]::before {
  animation: clawd-yawn-move 900ms ease-out forwards,
             clawd-yawn-frames 900ms step-end forwards !important;
}

/* B 轨姿势（在场表现）必须限定 owner，否则会抢 A 轨和 C 轨的画面 */
button.${BUTTON_CLASS}[data-clawd-owner="B"].clawd-yawning:not(.clawd-sleeping)::before {
  ...
}
```

`data-clawd-owner` 的取值是 `C` / `A` / `B`，由 `renderClawdTracks` 维护，
正好等于「谁在占画面」。B 轨的东西不加这条守卫，生成中它就会盖住 think/stream。

### 3.3 把原型的 keyframes 翻译过来

原型的关键帧写的是自定义属性（`--px --py --ptx --pty --pr`），要展开成完整的 transform：

```
transform: scale(.85) translate(<ptx>px, <pty>px) rotate(<pr>deg) scale(<px>, <py>);
```

**四条不能漏：**

1. **最外层那个 `scale(.85)` 必须留着**，那是 `::before` 的基准缩放。漏了姿势一开始整只会突然变大。
2. **每个关键帧都要写全五个值。** 原型里「某个属性在这一帧没写」意味着它在前后两帧之间
   **插值**，不是保持不变，也不是回到默认值。手算容易错，用上一轮那个展开脚本的思路
   （在 §5 有说明）。
3. **`@keyframes` 里不许写 `!important`** —— 按 CSS 规范整条会被忽略。
   这条最阴：动画名挂上了、时长也对、控制台一声不吭，但 transform 和换帧一个都不生效。
   只有跑进真浏览器量 computed style 才看得出来。上一轮我在这上面栽了 91 处。
4. **换帧和位移必须拆成两条动画**：位移走缓动，换帧走 `step-end`。
   合成一条的话 `box-shadow` 会在两帧之间插值，像素画会糊成一团。

### 3.4 验

```powershell
node --check index.js
node tools\test-2.0.118-regressions.js
node tools\test-2.0.118-runtime.mjs
node tools\test-a2-interaction.mjs
```

目检姿势的话，别在欢迎页验（见 §4）。

---

## 4. 这几轮踩出来的坑

**`#send_form` 的未知子节点在欢迎页会被 `display:none`。**
拿一个什么都没有的 `<span>` 试也一样。只在 `body.clawd-welcome` 时成立，真实聊天页里正常。
所以 `.sym`、`.shadow`、输入框装饰层这几个**要加 DOM 节点**的东西：能做，
但**必须在真实聊天页里验**，在欢迎页会以为自己写错了。
（具体是哪条规则我没找到，是靠逐张关样式表二分定位到 `claude-integrated-theme-live-style` 的。）

**气泡不能靠加容器来跟随。** 同上一条的原因。现在的做法是直接给气泡本身写独立的
`translate` 属性——它的 `transform` 被 `translateY(-50%)` 和入场动画占着，写 transform 会顶掉入场动画。

**代次令牌只能单调递增。** 清零会让令牌被复用，上一轮排队中的回调会认领新的一次动作。
生气和跺脚在半空触发就是这么来的。测试里有断言卡住。

**预热的几何会永久过期。** `ResizeObserver` 只在**尺寸**变化时触发，而聊天内容加载完之后
输入框是**位置**变了、尺寸没变。任何「挂载时量一次然后一直用」的几何都要加有效期。

**滚动是高频事件。** `trackSwipeArrows` 在生成期间吃掉 3823ms 是真实测出来的。
任何挂在滚动上的东西都要有死区、要把缓动交给 CSS、要在生成中整个跳过。

**别在 `pointermove` 和 `requestAnimationFrame` 循环里读布局。** 按下时读一次是允许的。

---

## 5. 一个能省事的工具思路

上一轮把 A2 的 19 条 keyframes 从原型翻译过来时，我写了个一次性脚本：
解析原型的 `@keyframes`，按属性各自出现的关键帧做线性插值，补齐每一帧的五个值，
再拼成完整的 `transform`。手算 60 多个数字必错。

那个脚本没留下来（当时是一次性用的），但思路值得复用：
**先把「原型的自定义属性关键帧」→「完整 transform 关键帧」这一步脚本化，再批量翻译。**
§2.1 那 12 个姿势加起来大概 20 条 keyframes，值得先花二十分钟写这个转换器。

---

## 6. 建议的顺序

1. ~~**§2.1 里不用加帧的那批**~~ —— 已完成并接入实际触发
2. **`applyTired` 疲劳**——下一步把 12 秒后的离散 `sit` 扩成连续变化；
   生成态目前仍只有 think / stream / sit 三档
3. **`.sym` 符号层**——做完之后 `tilt` / `wow` / `lean` / `around` 这些才完整
   （它们的表达一半在符号上）
4. 剩下要加帧的姿势，按你和 lulu 想先看到哪个来

`.shadow` 地面影子放最后：它要新增节点、要跟落地动画联动，收益主要是落地那一下的冲击感。
