# 2.0.36 发版说明：字体层级令牌层（FR-4 第一步）

改的文件：`styles/day-pc.css`、`night-pc.css`、`day-mobile.css`、`night-mobile.css`。
`index.js` 没改。

---

## 一、这次改了什么

### 1. 建了字体层级令牌层（唯一的新东西）

写在四份 CSS 的 `:root` 块里，紧挨 `--cl-icon-size` 上方。

**字体族别名（两条，不是三条）：**

```css
--cl-font-reading:var(--cl-serif);   /* Newsreader */
--cl-font-ui:var(--cl-sans);         /* Hanken Grotesk */
```

指向没变，只是起了语义名字。需求文档要的第三族（Manrope 做标题）没引入，
真要加时在这里加 `--cl-font-title`。

**六档界面字号：**

| 令牌 | 值 | 用在哪 |
|---|---|---|
| `--cl-fs-display` | `clamp(26px, 3.1vw, 40px)` | 欢迎页大标题 |
| `--cl-fs-lg` | 16px | 问候 toast |
| `--cl-fs-md` | 15px | 侧栏抽屉标签、手机端新建对话 |
| `--cl-fs-sm` | 14px | 抽屉内容、菜单项、角色名 |
| `--cl-fs-xs` | 13px | 用户名、近期对话名、空态提示 |
| `--cl-fs-meta` | 12px | 时间、次级操作 |
| `--cl-fs-micro` | 11px | 最小标签、代码条、订阅等级 |

**取值全部是从现有代码里已经在用的数归纳的，不是从需求文档抄的区间。**
所以替换后渲染结果逐像素不变。

**聊天正文不在这套档位里。** 它继续归 `--cl-pc-font-size` /
`--cl-mobile-font-size` 管，因为只有正文要乘酒馆的 `--fontScale`。
把正文塞进档位会让缩放滑杆再次失效。

### 2. 修了一处 `--fontScale` 漏乘（唯一的行为改动）

`#chat > .mes[type="assistant_message"] .mes_text` 原来是：

```css
font-size:.98em !important;
```

它的权重是 **(1,3,0)**，压过接 `--fontScale` 的那条 **(1,1,2)**
——id 数相同、类计数 3 > 1。结果：酒馆的字体缩放滑杆对
`assistant_message` 类型的消息在 PC 上完全无效。

手机端没这问题，因为 4945 行那条是 (1,3,2)，能赢。上一轮修手机时漏了 PC 这半边。

改成：

```css
font-size:calc(.98em * var(--fontScale, 1)) !important;
```

数值 `.98em` 不变，滑杆生效。跟上一轮 4940 行是同一类毛病：
**给 `.mes_text` 单独设字号、权重又比 `--fontScale` 那条高。**

### 3. 加了三处注释，没改代码

- 孤儿规则 `.mes_text { font-size:16px }` / `.mes_reasoning { font-size:13px }`：
  权重只有 (0,1,0)，在 `#chat` 里必定被压掉，目前不生效。留着兜底 `#chat` 之外的
  场合。注释里写明**别往上加权重**，一加就盖掉字体缩放。
- 手机端 `#send_textarea`：见下面第三节。
- `:root` 里注明哪些 px 是 Font Awesome 字形尺寸、不属于文字层级。

---

## 二、自查结果

把改动前后两份文件都去掉注释、再把令牌还原成字面值之后逐行对比：

```
day-pc.css      剩余实际差异 2 行
night-pc.css    剩余实际差异 2 行
day-mobile.css  剩余实际差异 2 行
night-mobile.css 剩余实际差异 2 行
```

四份文件的 2 行都是同一处 —— 上面第 2 条的 `--fontScale` 修复。
**其余 60 条 font 声明全是纯改名，零视觉回归。**

另外：四份文件大括号平衡，tinycss2 解析零错误。

---

## 三、发现但没动的三件事

### A. 手机端输入框的 iOS 防聚焦缩放保护已经失效（建议优先看）

`@media (max-width:700px)` 里写着：

```css
html body #send_textarea {
  /* Keep 16px on iOS to prevent focus zoom. */
  font-size:16px !important;
}
```

但 `html body:not(.clawd-welcome) #send_textarea` 那条是
`font:400 15px/1.35 ...`，权重 **(1,1,2) > (1,0,2)**，实际生效的是 **15px**。

iOS Safari 在输入框字号小于 16px 时，点击会整页放大。**那条保护现在是空的。**

改成 16px 会让输入区变高、挤压聊天区，属于可见改动，所以这次只加了注释。
要修的话就一次只动这一条，改完在 iPhone 上点一下输入框确认不放大。

### B. 41 条 em 型 font-size 没动

`.98em`、`.86em`、`.78em` 这类是就地微调，值互相之间没有规律，
归档就会改尺寸。这次按「只改对得上号的」原则整条跳过。

### C. 图标字号没动

`#send_but`(14px)、`.mes_button`(12px)、badge(9px) 这些是 Font Awesome
字形尺寸，不是文字，归 `--cl-icon-size` 那条线。硬塞进文字档位会把两套
尺寸系统搅在一起。

还有 `.clawd-rail-brand` 的 `font:300 23px/1 var(--cl-serif)`（品牌字），
和 `10.5px` / `11.5px` / `12.5px` 几个不落档的值，都原样保留。

---

## 四、发版清单（git 部分你自己跑）

1. 新建 `loader-2.0.36.js`，内容：

```js
/* Versioned entry point: typography token layer (FR-4 step 1). */
import './index.js?v=2.0.36';
```

2. `manifest.json`：`"js"` 改成 `"loader-2.0.36.js"`，`"version"` 改成 `"2.0.36"`

3. `index.js` 第 258 行 `CLAUDE_KEYBOARD_BUILD.id` 换成：

```js
id: '2.0.36-type-tokens-' + CLAUDE_THEME_VARIANT + '-' + CLAUDE_LAYOUT + '-ext',
```

4. 推送后在酒馆点**「重新安装」**（不是「检查更新」）

5. 验证：侧栏底部构建标识必须变成 `2.0.36-type-tokens-...`。没变就是还在吃缓存。

---

## 五、上线后验一遍

1. **侧栏、用户名、近期对话、toast、代码条** —— 字号应该跟 2.0.35 完全一样。
   有任何一处变大变小，就是某个令牌取值取错了，回来查上面的档位表。
2. **酒馆设置 → 字体缩放滑杆** —— 拖动时，PC 上的 assistant 类型消息现在应该
   跟着变。这是这次唯一该看到变化的地方。
3. 日/夜、PC/手机四种组合各扫一眼。四份 CSS 改的是同一批内容，
   但 PC 和手机的选择器写法不同（手机大量用 `font:` 简写），值得分开看。
