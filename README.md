# Claude Web 2.0 Test

## 2.0.142 测试版

- 修正 Android/Via 键盘上方出现大段空白：加入不可见、不可交互的顶部 pan anchor，并在输入框聚焦时重挂一次，让 Via 冷启动与热更新都使用正确的 IME 可见区域，不再把 fixed 输入框抬高两次。
- 原生键盘布局现在也会进入 composer 清理分支，主动移除旧版本或热更新遗留的负位移。
- 加入 Android 运行时回归：先向输入框外壳写入 `-272px` 的旧偏移，再触发 focus/resize，必须清理为零；A2 拖拽与双 Clawd 不改位。

## 2.0.141 测试版

- Clawd 角色本体加入抓起、拖动、抛掷、墙体反弹、首次触地压扁和落地记位；拖拽期间关闭浏览器平移与旧 transition，避免 Android 页面跟滚和橡皮筋。
- 加入五档连续戳击、被丢次数、烦躁衰减、跺脚与转身生气序列；原子序列使用代次令牌和单入口锁，不新增第二套状态计时器。
- 8 套主题帧数据由同步脚本补齐 14 帧，A2 的 12 个姿势继续复用现有 box-shadow 点阵；输入框右上装饰 Clawd 保持不动。

## 2.0.139 测试版

- 恢复 `2.0.135` 的消息末尾小 Clawd：桌面与手机尺寸、消息落款锚点、点击反馈、视线和打盹行为均保持原样，不合并、不放大。
- 输入框大 Clawd 独立保留在右侧，并把右侧手机安全区纳入纯 CSS 锚点；它继续跟随输入区与软键盘移动，不读取布局。
- 两只 Clawd 共用唯一的 A/B/C 状态源：大 Clawd 表演生成与当前状态，小 Clawd负责回复落款和原有互动；手机顶栏与 typing indicator 不再生成第三只。

## 2.0.138 测试版

- 修正「关闭粒子与提示气泡」会连 Clawd 本体一起隐藏的问题；现在只有「显示 Clawd」控制本体显隐。
- 允许 `#form_sheld` 显示越过输入框上沿的 Clawd，避免 Android WebView 裁剪。
- 单实例在渲染时写入最终显隐，防止后加载的旧落款样式在 Via 中把它覆盖成 `display:none`。

- Clawd 从消息落款、手机顶栏和 typing indicator 三套实例收口为 `#send_form` 上沿的一只真实按钮，键盘开合继续跟随输入区本身。
- 加入 A 生成轨、B 在场轨、C 触碰轨，固定使用 C > A > B 的画面优先级，并用生成轮次围栏避免结束、停止事件重复结算。
- Clawd 的闲置检查、A/C 超时和生成计时显示并入一个 200ms tick；拖拽、抛掷、五档戳击与主动行为留到后续批次。

## 2.0.135 测试版
- 修复 Claude 桌面侧栏遮挡第三方全屏弹窗：按弹窗实际几何范围检测，打开时侧栏退到遮罩后，关闭后自动恢复，电脑与平板共用同一规则。
- TT Prompt Manager 的三横拖动键只保留三个真实横杆，移除重复的背景图形，消除重影。

## 2.0.130 测试版

- TT 会保留一个空的原生 `.drag-handle`。现在直接复用、标记并绘制这个节点，不再把它误判成 ST 的完整原生拖动键；ST 原生节点仍不改动。

## 2.0.129 测试版

- TT 的拖动键与三条横线使用节点自身的高优先级样式，避免主题后写规则再次清空图形；横线仍位于真实 `.drag-handle` 触摸节点内。

## 2.0.128 测试版

- TT 的三横拖动键改用三个真实横条元素绘制，避开后写主题规则对文字与背景图的清空；ST 原生拖动键仍保持不变。

## 2.0.127 测试版

- TT 的拖动键改为在真实触摸节点上绘制三条横线，不再依赖 WebView 字体或伪元素；保留独立的拖动键、类型图标、名称三段布局。

## 2.0.126 测试版

- 提高 TT popup 内四列网格与拖拽手柄定位规则的优先级，覆盖酒馆原有双 ID 布局规则；仅处理扩展补回手柄的行。

## 2.0.125 测试版

- TT 补回拖拽节点后，缺节点的行改用四列网格，确保三横拖拽键、类型图标、名称和右侧控制区互不重叠。

## 2.0.124 测试版

- 拖拽手柄修复直接绑定唯一的 `#completion_prompt_manager_list`，兼容 TT 把列表移出原面板、挂到 popup 根节点的 DOM 结构。

## 2.0.123 测试版

- Prompt Manager 继续避开昂贵的聊天全量刷新，但列表重建时会单独执行一次轻量拖拽手柄修补，解决 TT 打开面板后仍无三横键的问题。

## 2.0.122 测试版

- TauriTavern 2.2.0 未生成 Prompt Manager 的原生 `.drag-handle` 节点时，按行补回真实拖拽手柄；SillyTavern 已有节点不会重复添加。
- 保留 2.0.121 的消息操作浮层，以及“拖拽键、类型图标、名称”三段式列表布局。

## 2.0.121 测试版

- 角色消息的省略号展开区改为可换行的浮动操作面板，恢复所有未被酒馆显式隐藏的真实动作，不再只显示复制和朗读。
- TT Prompt Manager 恢复左侧三横线拖拽手柄，同时保留类型图标与名称的独立列。

## 2.0.120 测试版

- CSS 性能清理只处理 SillyTavern 核心 `css/toggle-dependent.css` 中的目标规则，不再删除第三方扩展或 `user.css` 的 `[style]` 选择器。
- 保留长聊天的核心规则性能优化，同时恢复沉浸式单回合、传统多楼层等插件依赖的消息锁定样式。

## 2.0.119 测试版

- 恢复用户消息编辑入口，以及助手消息用于展开完整操作区的省略号；用户入口只转发给酒馆原生编辑功能。
- 省略号展开后继续交给 SillyTavern 原生逻辑收起，不让全部扩展按钮默认常驻。
- Prompt Manager 的类型图标和名称改用独立网格列，避免图标压住文字。

## 2.0.118 测试版

- Android 输入框改为完全跟随 WebView 原生键盘缩放，不再叠加 `visualViewport` 位移。
- 消息操作栏恢复省略号折叠；展开后仍保留 SillyTavern 与第三方扩展的全部真实按钮。
- 手机抽屉恢复独立顶栏占位，修正角色卡页空白和用户设置标题挤压。
- Prompt Manager 的类型图标改为固定图标列，不再压住提示词名称。
- TT 关闭全屏抽屉过渡动画，并降低移动端全量刷新频率。

## 2.0.117 测试版

- 消息操作栏改为直接显示 SillyTavern 与第三方扩展提供的真实按钮，恢复分支、检查点、绘图、翻译和扩展动作；编辑状态仍隐藏普通操作栏。
- 新增“引号文字跟随正文颜色”小开关，默认开启，关闭后恢复主题强调色。
- 扩展停用或卸载时恢复进入前主题，避免刷新后侧栏、聊天区继续消失。
- Character Manager 打开时临时隔离 Claude 强制样式，关闭后恢复，不再污染管理器布局。
- Prompt Manager 的删除入口扩大触控区并提高可见度；TT 编辑弹窗使用键盘前独立高度锁。
- Quick Reply 工具栏在手机端回到输入框自己的布局流，不再漂到页面左上角。

## 2.0.116 测试版

- 测试版显示名、重新安装地址和安装目录独立使用 `claude-web-test2`，不再覆盖正式版 `claude-web`。
- 正式版与测试版可以同时安装；由于两者都会接管同一套 SillyTavern 页面结构，请只启用其中一个。

## 2.0.84 测试

- 欢迎态单独归零 `#chat` 与 `#form_sheld` 容器，阻止主题位移欢迎结构；真实消息后代仍不进入归零范围。
- 发送与停止按钮保留原生显隐，同时锁定按钮变换，六主题下不再出现单个按钮缩放偏差。

## 2.0.83 测试

- 归零规则的整个作用域改为零特异性，确保框架正向声明总能覆盖归零默认值。
- 停止键、发送键及左右槽内部的酒馆原生控件退出归零子树，保留原生生成状态与隐藏状态。
- 表面背板改用不透明系统底色叠加主题色，透明主题不再穿透，同时保留主题色调。

## 2.0.82 测试

- 归零目标改用低特异性的 `:where()`，避免抽屉后代归零规则反过来压住框架自己的用户区、导航和输入框正向声明。

## 2.0.81 测试

- 兼容框架改用 `@layer` 固定结构层与外部主题层的优先关系，不再依赖样式节点先后顺序。
- Claude Web 自有节点使用 `all: revert-layer` 整体归零，删除逐属性防御补丁；主题换皮改走变量、标签文字和侧栏图标通道。
- 侧栏、抽屉、输入框与欢迎态增加框架背板；主题决定颜色，框架保证重新定位后的表面不透明。
- 生成器新增结构规则覆盖率断言，静态检查新增层叠边界、归零范围和防御属性回归检查。

### 兼容框架的换皮边界

兼容模式会锁定侧栏、Recents、用户区、欢迎页和输入框的结构。外部主题不能再通过这些节点的选择器改写尺寸、定位或槽位；颜色和字体应通过 `--SmartTheme*`、`--mainFontFamily` 或主题变量提供，侧栏图标仍允许使用 `background-image`、`mask-image`、伪元素 `content`，标签文字也保持可替换。`#chat` 内的真实消息正文不归零，仍由外部主题完整接管。

## 2.0.80 测试

- 兼容框架重新接管 Claude 欢迎页、侧栏完整结构和输入框完整结构；外部 JSON 继续完整接管真实对话正文。
- Recents 改用稳定的头像、标题、摘要与操作四槽；用户区固定到底部，长文本不再铺满侧栏。
- 输入框固定左右按钮与 textarea 三槽，快捷回复独占上方一行，停止键显隐交回 SillyTavern。
- 兼容 CSS 改为按结构属性生成，并加入消息选择器边界检查；窄屏暂时自动回退到完整手机版。

## 2.0.79 测试

- 页面在后台加载时改用定时器完成首次刷新，避免暂停的 `requestAnimationFrame` 把扩展永久卡在未初始化状态。
- 页面恢复可见时取消悬空帧并重新调度；卸载时同时清理刷新帧和节流定时器。

## 2.0.78 测试

- 兼容框架会保留外部主题提供的侧栏图片图标；主题只把原图标染成透明时，则恢复可见的默认图标。
- 输入框的发送与停止按钮由框架绘制稳定的箭头和方块，不再被外部主题的 Font Awesome 规则一起隐藏。

## 2.0.77 测试

- 约束侧栏搜索与收起按钮的 SVG 尺寸，避免外部主题的全局 SVG 规则把它们放大成黑色图块。

## 2.0.76 测试

- 重做框架兼容模式：保留 Claude Web 左侧栏、内容区让位和输入框。
- 欢迎页、消息卡片、侧栏图标、配色与背景继续由当前酒馆美化 JSON 接管。
- 兼容模式不再向 `.mes`、`.mes_block`、`.mes_text` 注入消息操作区、落款或排版 class。

## 2.0.75 测试

- 新增“兼容框架”运行模式：保留外部 SillyTavern 主题，不再覆盖它的配色、字体、背景、消息皮肤和原生布局。
- 兼容模式只加载扩展功能组件所需的精简样式，并停用 Claude 侧栏重排、欢迎页和移动端外壳。
- 完整美化与兼容框架可以在扩展设置中切换，切换后刷新生效。

## 2.0.74 调整

- 扩展设置改为外观、布局、Clawd、背景、关于与更新五个折叠分区。
- 分区标题显示当前状态摘要；只在相关功能启用时显示附属选项，减少面板长度。

## 2.0.73 修复

- Android 键盘开合时，只允许正在显示的 SillyTavern 自动补全实例重新定位，避免隐藏 textarea 批量创建光标镜像并阻塞输入框画面更新。
- 兼容补丁仅在 Android 移动布局启用；检测到 SillyTavern 本体已带同等保护时自动跳过，扩展卸载时恢复原方法。

## 2.0.32 调整

- 撤掉手机 SVG 的固定粗描边，并重新生成更细的 22px PNG。

## 2.0.31 修复

- 手机图标直接绘制在 `.drawer-icon` 元素上，并彻底隐藏 Font Awesome 的伪元素字形。

## 2.0.30 修复

- 手机侧栏改用预渲染的 22px PNG，绕过 WebView 对小尺寸 SVG 的断线栅格化。

## 2.0.29 修复

- 手机 SVG 使用非缩放描边，避免 WebView 缩放后出现断线和灰色碎边。

## 2.0.28 修复

- 手机侧栏改用裁掉留白的专用 SVG，不再强行放大桌面素材。
- 背景抽屉先播放滑入动画，再绘制缩略图网格，避免首帧卡顿与黑屏闪烁。
- 撤回 2.0.27 对手机 Clawd 的强制定位，不再干扰点击动画。

## 2.0.27 修复

- 放大手机版侧栏 SVG 的实际图形，避免图标缩成零碎线段。
- 锁定手机版右上角 Clawd 的位置，输入和状态变化时不再上移。

## 2.0.26 修复

- 移除桌面欢迎语右侧由 `#chat` 顶部内边距触发的无效滚动条。

## 2.0.25 修复

- 桌面抽屉关闭期间保持完整视口高度，避免原生关闭态先压成 52px 顶栏残影。
- 夜间模式使用浅色线条图标，保留橙色强调；桌面与手机均适配。

## 2.0.24 修复

- 桌面抽屉改为整块平移回侧栏后方，标题不再停留到动画末尾。
- 搜索与侧栏装饰图标移动到左侧栏标题的右上角。

## 2.0.23 修复

- 移除关闭抽屉残留的右侧滚动条，并恢复完整的拉回动画。
- 桌面侧栏图标放大，移除米黄色底板；桌面右上角加入透明底的搜索与侧栏装饰图标。

## 2.0.22 修复

- 侧栏分隔线改由高层固定 grip 绘制，抽屉开合时不再短暂覆盖边线。

## 2.0.21 修复

- 背景入口兼容新版酒馆的抽屉 ID，按 `fa-image` 系列 class 直接加载背景 SVG。

## 2.0.20 修复

- 桌面抽屉关闭时内容同步淡出，左侧标题不再晚于主体残留。

## 2.0.19 修复

- 侧栏 SVG 改为由原生图标槽直接渲染，移除黑色 mask 方块和重复的小图标。

## 2.0.18 修复

- 侧栏 8 个自定义 SVG 完整替换旧图标，不再与 Font Awesome 图标叠加。

## 2.0.17 修复

- 手机抽屉改为只使用 GPU 位移动画，移除全屏 `clip-path` 逐帧重绘。
- 隐藏抽屉不再长期占用 `will-change` 合成资源，切换更轻。
- 新增“自动主题”组件：关闭、跟随手机系统、按自定义时间切换。
- 按时间模式默认 07:00 进入日间、19:00 进入夜间，两项都可修改。

## 2.0.16 修复

- 手机端输入栏为 QR 助手和发送键预留完整的 78px 网格列，按钮不再溢出或被裁切。

## 2.0.15 修复

- 手机侧栏 Claude 与汉堡按钮回到同一顶栏
- QR 助手按钮在手机聊天输入框内恢复显示
- 角色选择列表恢复纵向布局；真正的 HotSwaps 收藏栏支持左右滑动
- 桌面与手机抽屉补上关闭返回动画

## 2.0.14 修复

- 桌面设置抽屉从侧栏后方展开，并占满侧栏以外的右侧区域
- 修复手机端 Claude 标题位移、楼层号压头像、输入助手隐藏、角色头像条不能横滑、思维链不能内部滚动
- SillyTavern-Layout 开启“输入时底栏布局”时，保留 QR 助手按钮

Claude Web 的稳定 2.0 版本，已经整理成可直接安装的 SillyTavern 第三方扩展。

这个仓库只发布 2.0。正在开发的 3.0 不会从这里推送。

## 安装

安装前，先停用酒馆助手里旧的 Claude 2.0 全局脚本。两个版本同时启用会互相覆盖。

1. 打开 SillyTavern 的「扩展」面板。
2. 选择 **Install extension**。
3. 粘贴下面的仓库地址：

```text
https://github.com/claudenoshujin/claude-web-test2
```

4. 安装完成后刷新页面。

## 功能

- 日间 / 夜间主题
- 自动 / 桌面 / 手机布局；自动模式跨过 700px 时立即切换
- 配色预设和自定义颜色
- 统一尺寸的桌面设置菜单
- 扩展面板内检查更新和重新安装

主题可以立即切换。手动更改布局后扩展会自动刷新。

## 2.0.13 修复

- 为电脑和手机设置抽屉补上真正的左侧滑入动画；每次打开都会重播
- 桌面关闭状态保留在屏幕外，避免酒馆原生 `display:none` 吞掉动画起始帧

## 2.0.12 修复

- 修复桌面抽屉仍然直接出现的问题：改为由 `openDrawer/closedDrawer` 状态驱动的左侧滑入过渡
- 更新扩展入口和 CSS 缓存标识，避免 TauriTavern/浏览器继续加载 2.0.11 的旧样式

## 2.0.11 修复

- 移除主页顶栏的额外 Claude 标题，侧栏打开时只保留侧栏自己的 Claude 标题
- 手机和桌面设置抽屉统一从左侧进入；桌面扩展面板不再单独变宽
- 设置页内容避开 56px 手机顶栏，顶部保持干净留白

## 2.0.10 修复

- 手机设置抽屉改为全视口宽度，从左侧横向滑入。
- 抽屉顶部默认避开手机顶栏选择区，不再被顶栏遮住。
- 抽屉内容禁用酒馆原生的纵向下拉动画，避免上下拉入和横向菜单叠加。

## 2.0.9 修复

- 流式生成时思维链可以正常展开并查看，结束生成后不再被自动收回。
- 手机上翻旧楼层时，离屏的回复切换按钮不再拦截滚动；拖动手势不会误触重新生成。
- 桌面扩展面板恢复 1040px 宽布局，复杂设置不再挤在窄双列里。
- 手机端角色头像完整显示；欢迎页右侧控件在第二行垂直居中。
- 角色卡批量删除的选中项恢复高对比边框和底色。
- 手机汉堡菜单与魔法棒菜单使用同一套外框、圆角和阴影。

## 2.0.8 修复

- 手机端角色头像不再被纵向拉伸裁切（头像盒子多留的高度只用来对齐标题行，头像本体现在钉死为正方形，不再"卡一半"）
- 正文字号现在真正接入酒馆原生"字体缩放"滑杆：以前这个滑杆对聊天正文完全没有效果，PC 和手机的正文/思维链字号都会跟着滑杆同步放大缩小

## 2.0.7 更新

- 手机聊天视口避开固定输入框，文字不再被输入框上沿切掉
- 连续点击 Clawd 三次可切换持续抖腿状态，再点三次恢复
- Clawd 增加输入框聚焦和有文字时的状态反馈
- 统一生成中、睡眠、常驻状态和 reduced-motion 的动画优先级
- 设置面板增加 Clawd 动效与粒子提示开关
- 增加日间、夜间一键切换按钮

## 2.0.6 修复

- 清除桌面与手机顶部重复预留的 35px 空白层
- 清除输入框外壳额外绘制的上方实体边框

## 2.0.5 修复

- 手机端角色头像只占标题行，不再挤窄整条回复
- 手机端用户气泡移除多余的右侧按钮占位
- 手机端角色正文、思考区和操作按钮统一左边线

## 2.0.4 修复

- 清除用户与角色消息进入编辑状态后的工具条投影和灰度滤镜
- 移除用户设置分组标题的深色渐变
- 重新设计用户设置滑块的浅色轨道和强调色圆点

## 2.0.3 修复

- 清除角色消息操作图标继承的酒馆原生黑色投影
- 用户消息与角色消息的操作图标保持同样清晰

## 2.0.2 修复

- 修复新版 SillyTavern 中普通消息仍显示编辑工具条
- 修复中等宽度窗口里设置菜单只剩标题栏的问题
- 设置菜单保持统一最大宽度，空间不足时自动收窄
- 扩展面板空间不足时从双列自动改成单列，避免按钮和图标挤压

## 2.0.1 修复

- 浏览器拉伸跨过桌面 / 手机断点时自动适应
- 所有桌面设置菜单统一为同一宽度
- 撤下未完成的档案版式和相关选项
- 修复普通消息错误显示编辑工具条
- 修复普通按钮和编辑按钮重叠

## 更新

在 SillyTavern 的 Claude Web 扩展设置里选择：

- **检查更新**：正常更新时使用
- **重新安装**：检查更新报 HTTP 500 时使用

部分环境能安装扩展，但没有可供更新接口调用的系统 Git。这时重新安装即可。主题和配色保存在浏览器的 localStorage 中，重新安装不会清除这些设置。

## 手动安装

把仓库内容完整放进：

```text
SillyTavern/public/scripts/extensions/third-party/claude-web-test2/
```

`manifest.json` 必须位于扩展目录根部。

## 版本

当前版本：`2.0.8`
