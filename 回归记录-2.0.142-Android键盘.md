# 2.0.142 Android 键盘回归记录

日期：2026-08-21

## 已修问题

### 输入框与键盘之间出现大段空白

- 现象：Android 打开软键盘后，输入框停在屏幕中上部，底部到键盘顶部约有 270px 空白；两只 Clawd 跟着输入框一起悬空。
- 根因一：Via 普通页面打开键盘后仍报告 `innerHeight=745 / visualViewport.height=746 / offsetTop=0`，Android 在 Web API 不知情的窗口层平移整张 WebView，fixed 输入框因此被抬高两次。
- 根因二：`#form_sheld` 还可能保留旧版本或热更新写入的负 `--cl-mobile-composer-translate-y`。Android 调度器提前返回，导致已有清理分支无法执行。
- 修复：Android 页面安装一个空的、不可交互的 64px 顶部 fixed pan anchor，让 Via 使用正确的 IME 可见区域；输入框 focusin 时会在同一任务内重挂该节点，确保冷启动也进入 Via 本次 adjustPan 计算；同时每次 focus、resize 和 viewport 变化都清理遗留负位移。
- 隔离结果：1px anchor 无效，64px anchor 能把输入框从屏幕中段恢复到键盘上沿，间距约 8px；`overlaysContent` 在当前 Via 中写入后未生效，未作为修复保留。
- 防回归：运行时测试验证 pan anchor 的高度与不可交互属性，并先人工写入 `-272px` 残留值，再确认 Android focus/resize 后该值为空。

## 回归范围

- 本地语法、静态结构、运行时 DOM、A2 交互及差异格式检查。
- Android Via：欢迎页与聊天页、键盘连续开合、输入框贴边、双 Clawd、安全区、聊天滚动、抓取和抛掷。
- 正式仓库不在修改范围内。

## 环境限制

- 模拟器当前显示 `Not Connected to API!`，无法把真实流式生成、停止和错误事件加入手工回归；这部分只由运行时事件测试覆盖。
- 浏览器插件在本机连接本地页面时被自身的 trusted-code-path 校验拦住，所以桌面端没有做浏览器截图；本地服务已确认返回 HTTP 200，桌面路径由静态、运行时 DOM 和兼容结构测试覆盖。

## 回归中发现的测试问题

- 运行时测试原先只等 1050ms 就断言 `think → stream`。产品逻辑是 900ms 门槛加 200ms tick，最坏相位接近 1100ms；已把测试窗口改为 1250ms，产品状态时长不变。
- 最后一次真正 force-stop Via 后发现：pan anchor 在热更新场景有效，但启动阶段就存在的 fixed 节点没有被 Via 纳入首次 IME 计算。已改为 focusin 同一任务重挂，并增加“旧节点必须断开、新节点必须接管”的运行时断言；修后重新 force-stop 冷启动，再连续开合键盘两次均通过。

## 最终验证结果

- 本地 SillyTavern：`http://127.0.0.1:8000/` 返回 HTTP 200，页面正文包含 SillyTavern。
- Android 欢迎页：force-stop Via 后冷启动加载 2.0.142，第一次打开键盘时输入框距键盘工具栏约 8px；关闭再打开一次结果相同。
- Android 已有聊天：打开 Seraphina 历史消息后输入框继续贴住键盘；上下滚动消息时输入框不跟随内容漂移。
- 双 Clawd：左侧交互 Clawd 可在键盘打开时独立拖动并保留落点，右侧装饰 Clawd 固定在输入框右上；两者都没有遮挡 `+`、模式菜单或发送按钮。
- 关闭键盘：输入框回到手机底部安全区上方，没有遗留负位移。
- 自动化：语法、focused regressions、runtime DOM、A2 交互、consistency、compat structure、day/night coverage 与 `git diff --check` 均通过。

## 仍需人工确认

- 连接真实 API 后观察一次完整的生成、停止和错误流程，确认 A/B/C 状态动画的主观节奏；本轮只验证了同一状态源的运行时事件，不具备真实网络流。
- 在至少一台非 Via 的 Android 真机浏览器复核 64px pan anchor。它只在 Android 移动布局安装且不可点击，但不同厂商 WebView 的 adjustPan 策略仍可能不同。
