# 给 Claude 的诊断报告：2.0.143 Clawd 高度限制与抓取、回落卡顿

> 版本：测试仓库 `2.0.143`，提交 `e351bed`
> 环境：Android 模拟器，Via 浏览器，本地 SillyTavern
> 附件：`codex-clipboard-4c1f34da-ca62-4660-b209-521033cf387c.png`（请和本文一起交给 Claude）
> 本文只做诊断和复现说明，没有修改产品代码。

## 一句话结论

这是两个同时存在的问题，不是同一个问题：

1. **Clawd 向上的活动范围被硬编码为 140 CSS px。** 截图里的“拉不到更高处”是确定缺陷，不是手势误差。
2. **回落物理按“每帧”计算，而不是按真实经过时间计算。** Android WebView/Via 一旦降到 30fps，回落墙钟时间约变成 60fps 时的两倍；聊天页面卡顿时会更慢。这是当前“回落很慢”的最强根因。
3. **拿起来的卡顿目前是 Android Via 特有或显著放大的问题。** 同一份本地酒馆在电脑端用鼠标抓取时顺滑，因此共享状态源、基础拖拽算法和 340ms/350ms 时间配置不太可能单独造成卡顿。优先检查 Android 的触摸事件派发、WebView/Via 帧率、主线程绘制和合成；仍需两端时间线才能定责。

这也修正了 2.0.143 的判断：`touch-action` 和首次布局读取确实值得处理，但它们不是这次真实设备卡顿的完整根因。

## 用户看到的现象

截图中：

- 左侧可交互 Clawd 从输入框上方被拖起后，只能到达输入框约 140px 上方，无法进入用户红框标出的更高区域。
- 右侧输入框装饰 Clawd 仍固定在输入框右上方，这一职责没有混淆。
- 用户用真实手指操作时，抓起和回落都显得迟钝；回落尤其慢。
- 用户随后确认：同一份本地酒馆在电脑端抓取不卡。电脑端使用鼠标，Android 端使用触摸，这构成当前最重要的对照组。
- 截图时键盘关闭，因此这个高度限制不是软键盘安全区造成的。

## 新增的跨端对照意味着什么

电脑顺滑、Android Via 卡顿，并不推翻高度上限和逐帧回落的结论，但会重新排列抓取延迟的排查顺序：

- **服务器、网络和角色状态源优先级降低。** 两端使用同一份本地酒馆内容，拖动又主要发生在客户端；如果共享逻辑本身每次都会阻塞，电脑端通常也应复现。
- **340ms 抓起动画和 350ms 状态延时不是充分根因。** 它们会影响动作节奏，但相同配置在电脑端没有造成用户所说的跟手卡顿。它们仍可能在低帧率设备上放大问题。
- **Android 触摸路径优先级升高。** 鼠标和触摸的事件来源不同。需要比较 `pointerdown` 后首个 `pointermove` 的到达时间、事件合并情况，以及浏览器是否仍在进行手势判定。
- **Android 绘制和合成优先级升高。** 当前 Clawd 是大量像素 `box-shadow` 配合 CSS 动画。桌面 GPU 能顺畅绘制，不代表 Android WebView 能在同一帧预算内完成。
- **逐帧回落结论反而得到现实解释。** 电脑通常维持 60fps 或更高，所以问题不明显；Android Via 如果降到 30fps，现有物理会自然变成约两倍时长。

目前不能仅凭这个对照断定是 Via 的 bug，也不能断定是 Android 性能不足。它只能把问题定位到“触摸输入到屏幕显示”这段客户端链路。

## 已确认的代码事实

| 现象 | 代码事实 | 产品结果 | 结论强度 |
|---|---|---|---|
| 向上拖不高 | `index.js:2050` 定义 `A2_CEILING = 140` | 可交互 Clawd 最多只能比归位点高 140 CSS px | 已确认 |
| 高度不随屏幕变化 | `a2Walls()` 始终返回 `miny: -A2_CEILING`、`maxy: 0` | 大屏、小屏、键盘开关和聊天可视区都共用同一个 140px 上限 | 已确认 |
| 抓起显得慢 | 抓起动画 `clawd-a2-grabP/F` 为 340ms | 即使坐标已开始变化，角色造型仍会用三分之一秒完成抓起 | 已确认的时间配置，是否为主要瓶颈待测 |
| 悬挂造型切换晚 | `pointerdown` 后另有 350ms 定时器才进入 drag/dangle 状态 | 长按后动作会有明显的分段感 | 已确认的时间配置 |
| 回落随帧率变慢 | 弹道每帧执行 `vy += g`、`vx *= drag`、`vy *= drag`，没有使用 `requestAnimationFrame` 的时间戳或 `dt` | 30fps 的真实耗时约为 60fps 的两倍；15fps 约为四倍 | 已确认 |
| 落地拖长 | 落地动画为 820ms；最终稳定后还有 840ms 清理定时器 | 用户会继续看到“尚未完全结束”的落地状态 | 已确认 |
| 真实触摸仍卡 | 当前测试只测 JS 同任务内的布局读取和 transform 写入 | 没有覆盖浏览器事件派发、样式计算、合成和实际屏幕刷新 | 尚未定责 |

代码中的注释也把 140px 标为临时值，并写明应由真实酒馆页面测量替代。当前实现尚未完成这一步。

## 回落时间估算

按 2.0.143 当前参数 `A2.feel = 0.64`，可得到：

- 重力：约 `0.298 px/frame²`
- 空气阻尼：约 `0.986 / frame`
- 反弹系数：约 `0.460`
- 最多允许 6 次反弹；从当前高度通常发生 2–3 次

用与源码等价的逐帧计算，从不同高度释放：

| 释放条件 | 稳定所需帧数 | 60fps 墙钟时间 | 30fps 墙钟时间 | 再计入 840ms 稳定后清理 |
|---|---:|---:|---:|---:|
| 高 140px，初速度 0 | 67 帧 | 1.12s | 2.23s | 60fps 下约 1.96s；30fps 下约 3.07s |
| 高 140px，向上速度 -3 | 76 帧 | 1.27s | 2.53s | 60fps 下约 2.11s；30fps 下约 3.37s |
| 高 140px，向上速度 -6 | 87 帧 | 1.45s | 2.90s | 60fps 下约 2.29s；30fps 下约 3.74s |
| 高 70px，初速度 0 | 50 帧 | 0.83s | 1.67s | 60fps 下约 1.67s；30fps 下约 2.51s |

因此，“拿得越高，落得越慢”不只是距离增加。向上甩动会增加弹道帧数，而设备掉帧又会把每一帧拉长。两者会叠加。

## 2.0.143 已经排除到什么程度

2.0.143 已做过这些处理：

- 给交互目标设置静态 `touch-action: none !important`，避免浏览器先把手势当页面滚动。
- `pointerdown` 时立即进入 C 状态的 grab 轨道。
- 预热 A2 边界，正常 `pointerdown` 不再临时读取输入框几何。
- 首次有效 `pointermove` 先写 transform，再延后提示和粒子。
- 自动化用例确认上述路径没有同步强制布局读取。

这些结果只能说明“JS 处理函数里没有此前怀疑的同步布局堵塞”。它们不能回答以下问题：

- 真实手指的首个 `pointermove` 何时到达 Via。
- transform 写入后，浏览器何时完成样式计算、合成并显示到屏幕。
- C 状态像素动画、粒子或提示是否占用了主线程或绘制预算。
- 长聊天内容是否让 Via/WebView 的刷新率下降。

所以不应继续只凭桌面自动化调整阈值或 `touch-action`。下一轮需要先拿真实时间线。

## 建议 Claude 先做的诊断埋点

建议在电脑端和 Android Via 都添加同一份默认关闭、仅测试时启用的 A2 trace，不要先改手感参数。至少记录：

1. `pointerdown` 时间。
2. `setClawdC('grab')` 的时间。
3. 第一个 `pointermove` 到达时间，以及该事件的坐标。
4. 第一次 `a2Place()` 写 transform 的时间。
5. 写 transform 后连续两个 `requestAnimationFrame` 回调的时间，用来估算事件到可绘制帧的等待。
6. `pointerup` 时间。
7. 回落每一帧的 `dt`、总帧数、总墙钟时间、反弹次数和撞到边界的次数。
8. `pointermove` 事件总数；若浏览器支持，同时记录 `getCoalescedEvents()` 数量。
9. `grab`、`drag/dangle`、`land` 动画的开始和结束时间。
10. 若 Via 支持 `PerformanceObserver` 的 `longtask`，记录拖动前后所有长任务。

可以沿用现有键盘 trace 的接口风格，例如暴露：

```js
__claudeClawdInteraction.a2TraceStart()
__claudeClawdInteraction.a2Trace()
```

trace 中必须使用 `performance.now()`，不能只数帧。

## 建议的诊断 A/B，不是产品改法

先在同一存档、同一聊天位置做跨端基线：电脑端用鼠标测至少三次，Android 模拟器用真实触摸或宿主触摸输入测至少三次。然后在 Android 端继续以下 A/B：

1. 当前 2.0.143。
2. 仅临时关闭 `grab/drag/land` CSS 动画，保留位移逻辑。
3. 仅临时关闭粒子和提示，保留角色动画。
4. 临时把回落改成基于 `dt` 的诊断分支，其他参数不变。
5. 分别在短对话、截图中的长对话、键盘关闭和键盘打开时测试。

比较时重点看：

- `pointerdown → first pointermove`
- `first pointermove → first transform write`
- `first transform write → next rAF`
- 回落总墙钟时间与平均/95 分位/最大帧间隔

跨端比较时还要记录浏览器名称、设备刷新率、`devicePixelRatio`、可视区尺寸，以及两端是否实际加载了同一个提交 `e351bed`。如果电脑端和 Android 的静态资源版本不同，这组对照无效。

如果关闭角色动画后抓取立即变顺，瓶颈在动画绘制或视觉时间配置；如果事件本身晚到，优先查 Via 的触摸手势和页面滚动竞争；如果 transform 已及时写入但下一帧很晚，优先查主线程和绘制成本。

ADB 合成 swipe 可以用来验证坐标和边界，但不能替代真实手指的延迟结论；ADB 截图的抓取时机也不足以测几十毫秒级响应。

## 高度限制需要 Claude 明确的产品问题

不要只把 `140` 换成另一个更大的常量。请先确定：

- 可交互 Clawd 的上边界应是可视窗口顶部、聊天内容区顶部，还是导航栏下方。
- 它能否覆盖聊天文字和角色头像。
- 键盘打开时，上边界是否保持不变，只移动归位点；还是活动范围随可用高度缩小。
- 松手后是否始终落回输入框上方的归位线，还是允许停在聊天区。

从用户截图能确定的是：当前 140px 太低。截图中的红框只能证明用户在强调更高的可达区域，不能单凭这张图推断精确的新上边界。

## 修复后的验收要求

1. 交互 Clawd 的高度边界由实际可视区域计算，不再使用固定 140px。
2. 键盘关闭和打开时都不侵入系统安全区，不挡输入框的 `+`、发送键及右侧装饰 Clawd。
3. `pointerdown` 到角色首次可见位移用真实设备墙钟时间衡量，并报告多次测试的中位数和 95 分位。
4. 回落使用真实时间推进；30fps 和 60fps 下总墙钟时长应基本一致。
5. 抓起、悬挂、落地动画不能延迟坐标跟手，也不能让已经落稳的角色继续显得卡住。
6. 左侧交互 Clawd 和右侧装饰 Clawd 继续共用 A/B/C 状态源，但保持各自的视觉职责。
7. 现有页面滚动、输入框、键盘适配和状态测试不得回退。

## 请 Claude 回传的结果

请不要只回复“改好了”。请附上：

- 真实设备一次完整抓起、拖动、释放的 trace。
- 同一场景的电脑端 trace，作为顺滑基线。
- 修改前后 `pointerdown → first rAF` 和回落总墙钟时间。
- 新高度边界的计算规则，以及键盘开关时的边界数值。
- 是否确认动画绘制、事件派发或主线程长任务中的某一项是抓取延迟主因。
- 在 Android Via 中实际复测的视频或连续帧截图。
