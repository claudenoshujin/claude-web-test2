# Clawd A2：这一轮的修复、现状盘点、和分工

日期：2026-08-21（第二轮）
基线：`2.0.144` 测试版。**版本号、loader 和断言已经同步；Android Via 已验
键盘开合及 `tilt` / `wow`，A2 回落 trace 和完整抛掷回归仍待做。**

---

## 1. 这一轮修的四个 bug

都在真浏览器（你本机酒馆 `127.0.0.1:8000`）里量过。

| 你报的现象 | 真正的原因 | 实测 |
|---|---|---|
| 被发送键旁边那只装饰 Clawd 挡住，拖不过去 | 我上一轮给它加了避让，贴着归位线时右边界提前 66px。手感上那是一堵看不见的墙 | 去掉后能拖到 right=1389（窗口 1396），装饰在 1136–1172 |
| 触发生气时把空中的 Clawd 吸到输入框上；跺脚也在空中触发 | **代次令牌被复用**。`a2Down` 把 `A2.flying` 清零，于是下一次抛掷又拿到 `my=1`，上一轮还在排队的 840ms 回调（`my` 也是 1）会认领这次飞行 | 改成递增。同时删掉了「进锁就把它瞬移回归位线」那段 |
| 顶部没有边界 | 拖动时有上边界（上一轮加的），但**抛掷循环里只夹了左右和地面，没夹天花板**，用力往上一甩就飞出屏幕 | 加了天花板反弹，实测最高点停在 top=6 并弹回，最后落回归位线 300 |
| （顺手）在半空抓住又原地松手 | 会悬在空中不掉下来 | 现在会自己掉回去，且不计入抛掷次数、不加烦躁 |

`A2.flying` 那条是这一轮最值得记的：它是 §5 那条老坑「嵌套 setTimeout 被打断后旧链继续烧」换了个形式回来——
链是断了，但令牌被回收再发，旧链于是拿着一张有效的票。**代次令牌只能单调递增。**
测试里已经加了 `assert.doesNotMatch(src, /A2\.flying = 0/)` 卡住它。

三个测试全过（静态 / 运行时 / A2 交互），新增 5 条断言。

---

## 2. 你问的三件事，现状是这样

### 输入态：有，但弱到等于没有

代码里确实有：

- 输入框获得焦点 → Clawd 整只 `translate: 0 -3px`（抬起 3px）
- 框里有文字 → `::before` 播 `clawd-compose-bob` 900ms 循环

3px 的抬起在截图里根本看不出来，bob 是个很轻的上下浮动。所以你的判断是对的——
**不是没做，是做了等于没做。** 这是个设计问题，不是 bug，见 §5 的问题。

### 滚动态：完全没有

`index.js` 里 `scroll` 相关的姿势 0 处。原型里有 `p-scroll`，没移植过来。

### 输入框装饰层（节点花草树）：完全没有

`clawd-deco` 在 `index.js` 里 0 处。定稿文档（`Clawd输入框装饰-节点花草树-定稿-20260819.md`）
写得很完整，HTML 和 CSS 都能直接抄，是这三件里最能独立开工的一件。

**我替它排了一个雷**：往 `#send_form` 里加未知子节点，computed display 会是 `none`——
但这只在**欢迎页**（`body.clawd-welcome`）成立，真实聊天页里是 `block`。
所以装饰层能正常做，只是在欢迎页会跟着输入框的其它装饰一起被藏（应该是有意的）。
做的时候记得**在真实聊天页里验，别在欢迎页验**，否则会以为自己写错了。

---

## 3. 一个会烧掉整个 A2 的地雷

**`src/clawd-interaction.js` 里一行 A2 的代码都没有。**

```
src/clawd-interaction.js 含 a2Walls:          0
src/clawd-interaction.js 含 clawd-a2-grabP:   0
src/clawd-interaction.js 含 a2NoteInteraction:0
```

A2 的全部代码（我写的和 Codex 写的）只活在 `dist-extension/claude-web-test/index.js` 里，
而那个文件是 `tools/build-extension.js` 的**产物**。**谁跑一次构建，A2 就整个没了。**

这不是假想：`tools/sync-sources.js` 的注释里写着一模一样的事故——
「v1.2 之后所有改动都是直接补进 JSON 的，源文件停在 v1.1，谁跑一次 pack.js 就会把包退回 1.1」。
同一个坑，上一层。

两条出路，选一条就行：

1. 把 A2 回填进 `src/clawd-interaction.js`（`sync-sources.js` 的思路可以参考）
2. 给 `build-extension.js` 加一个守卫：dist 比 src 新就拒绝运行，并打印怎么处理

**这件事应该排在装饰层前面做**，因为装饰层又会往 dist 里加一大块只存在于 dist 的代码。

---

## 4. 分工

### 为什么这么分

这一轮所有非显然的 bug 都是**量出来的，不是想出来的**：侧栏是 `#top-bar`、
三个容器都铺在侧栏底下、预热的边界会永久过期、`#send_form` 在欢迎页藏未知子节点、
代次令牌被复用。我有一条直连你本机酒馆的浏览器通道，这类活我做得快。

Codex 有 PowerShell、adb、git 写、删文件——安卓和发版只有它能做，我一条都跑不了。

共同的硬约束：**`index.js` 是一个文件，两个人别同时改。**

### 我做 —— 已经做完了

| 任务 | 状态 |
|---|---|
| 输入态重做 | ✅ 聚焦 → 低头看（`look-d`）；框里有字 → 一边看一边点头。都限定 `data-clawd-owner="B"`，生成和被抓被丢时让位 |
| 滚动态 | ✅ 照搬原型：被滚动带歪（≤9°），滚得猛就换眯眼的帧。挂在**已有的** `handleChatScroll` 上，没有新增监听 |
| 断言 | ✅ 滚动态和输入态各一组，在 jsdom 里跑；三个测试全过 |

滚动态的四条性能约束（前三条抄原型，第四条是扩展这边加的）：

1. **死区**：平滑后的速度低于 2.2 直接返回。慢速滚动一分钱不花，绝大多数滚动走不到下面
2. **缓动交给 CSS `transition`**，JS 每次只写一个角度，不做逐帧动画
3. 只在 B 轨占画面时表演；被抓、在空中、生气、生成中都不表演
4. **生成中整个跳过** —— 那时正文每帧在长、DOM 一直是脏的，读 `scrollTop` 会摊上一次
   完整重排。`trackSwipeArrows` 在生成期间吃掉 3823ms 就是这么来的

倾斜角写在独立的 `rotate` 属性上：`transform` 被 A2 的位置占着、`translate` 被输入态的
抬起占着，三者互不干扰。这条在测试里有断言卡住。

**所以 `index.js` 现在可以交给 Codex 了**，装饰层不用再等。

### Codex 做

| 任务 | 为什么归它 | 碰不碰 index.js |
|---|---|---|
| A2 回填进 `src/`，或给构建加守卫（§3） | 要跑构建、要 commit | 不碰 |
| 版本号 2.0.144 + loader + 静态测试的三处断言 | 发版流程一直是它在做 | 碰一行 build id |
| git commit / push；删掉 `_to_delete\`；删掉酒馆里那个空壳 `claude-web-test\` | 我没有删除权限，git 写会留锁 | 不碰 |
| 安卓回归 + 诊断文档要的 A/B 和热路径埋点 | 我跑不了 PowerShell 和 adb | 后面碰 |
| 输入框装饰层（节点花草树） | 定稿文档完整，纯静态 HTML+SVG+CSS，不碰 A2 的任何状态 | **碰** |

### 顺序（因为 index.js 共享）

```
现在        Codex：§3 的 src 回填 / 守卫 → 版本号 → commit → 安卓回归
            我：输入态 + 滚动态
            （这段两边不冲突：Codex 不碰 index.js 的主体）

我交棒后    Codex：装饰层（那时 index.js 归它）
```

如果你更想先看到装饰层，就反过来：Codex 先做装饰层并 commit，我等它推完再动 index.js。
装饰层是独立一层、插入点明确，先做后做都行。

### 交接口

我交棒时会给：改动清单、新断言、以及输入态/滚动态各自的姿势 class 名和挂载点，
这样装饰层那一层不会跟它们抢选择器。

---

## 5. 已经定下来的（原来这里是待定项）

- **输入态**：低头 + 点头，两个都做了
- **滚动态**：照搬原型的跟随倾斜，配上面那四条约束
- **重叠**：不管。可交互 Clawd 现在可以停在装饰 Clawd 上面，这是去掉那堵墙的代价，
  用户明确选了「能停就停」

原来这一节的待定内容：

**输入态想要多明显。** 现在是抬 3px + 一个很轻的 bob。要做成一眼能看出来的话，
可选的方向：低头看输入框（视线跟着光标）、有文字时换一张「在看」的帧、
打字时轻微点头。这几种在噪音上差别很大，得你定。

**滚动态想表达什么。** 原型里 `p-scroll` 是跟着滚动方向倾斜。
但滚动是高频事件，做重了会跟 §3 那个 3823ms 的教训撞上。
保守做法是只在滚动开始/停止两个瞬间各给一次姿势，中间不跟。

**另外一个我替你做了、但你可能想改回去的取舍**：去掉装饰 Clawd 那堵墙之后，
可交互 Clawd 现在可以停在装饰 Clawd 上面（盖住它）。你说的是「拖不过去」是 bug，
所以我按能拖过去改了。如果你希望它能拖过去、但不要停在上面，
可以在落定时把它轻轻推开——那是另一段代码，说一声我加。
