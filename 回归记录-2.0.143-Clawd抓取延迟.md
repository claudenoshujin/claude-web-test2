# 2.0.143 Clawd 抓取延迟回归记录

日期：2026-08-21

## 用户现象

- Android 上抓起输入框左上方的交互 Clawd 时延迟很高，看起来像要先长按，之后才开始跟手。

## 根因

1. 可拖 Clawd 静止时是 `touch-action: manipulation`，直到 `pointerdown` 才以内联样式改成 `none`。Android 在手指落下前已经决定当前手势是否交给页面平移，监听器里再改对这次手势太晚。
2. `pointerdown` 同步读取 Clawd 和输入框外壳的几何来计算空气墙。长聊天或键盘刚重排时，这两次 `getBoundingClientRect()` 可能强制整页布局，直接推迟抓取姿势。
3. 第一次移动超过 5px 后，旧代码先创建台词气泡和两颗粒子，做至少三次几何读取，最后才写 Clawd 的新位置。聊天越长，这个顺序越容易把首次跟手帧卡住。

## 修复

- 可拖 Clawd 静止时固定使用 `touch-action:none !important`，在手势开始前就明确由 Clawd 接管，不再允许运行时晚切。
- `pointerdown` 立即写入 C 轨 `grab`；5px 阈值只判断松手后算戳还是抛，不再决定什么时候出现抓取反馈。
- 空气墙边界在 Clawd 挂载和输入框尺寸变化后的 rAF 中预热。正常 `pointerdown` 不读取布局；仅页面刚启动、预热尚未完成的极端首帧保留回退计算。
- 首次有效 `pointermove` 先写 `translate3d`，再通过 rAF 后的任务创建气泡和粒子，让位置先提交一帧。

## 自动回归

- 按下后必须立刻进入 `grab`。
- 正常按下路径几何读取次数必须为 0。
- 第一次有效移动必须同步写入非零 `translate3d`，同一任务的几何读取次数必须为 0。
- 首帧之后气泡和粒子仍会创建，验证不是直接删掉反馈来换性能。
- 3px 内仍判为戳；超过 5px 仍判为拖拽/抛掷；生气锁、衰减和落地序列保持原行为。

## Android 实测

- Via 冷启动欢迎页：2 秒慢拖，在拖拽开始约 100ms 的分时截图中 Clawd 已离开原锚点；欢迎标题没有被拖动手势滚走。
- 已有 Seraphina 长聊天 + 键盘打开：约 100ms 截图中 Clawd 已跟随手势移动；消息位置不变，输入框仍贴住键盘，右侧装饰 Clawd 不动。
- `+`、模式菜单、发送按钮和手机安全区没有被拖拽改动遮挡。

## 仍需人工确认

- ADB 注入手势和分时截图能证明首帧已响应，但不能代替真实手指的主观延迟判断。需要用户在当前已打开的模拟器上直接抓一次，确认手感。
- 模拟器仍未连接真实 API；流式输出同时发生时的手感由 DOM/A2 自动回归覆盖，未做真实网络生成实测。
